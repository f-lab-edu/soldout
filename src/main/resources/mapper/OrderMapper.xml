<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="api.soldout.io.soldout.mapper.OrderMapper">

  <insert id="insertOrder" parameterType="api.soldout.io.soldout.dtos.entity.OrderDto" useGeneratedKeys = "true" keyProperty="id">
    INSERT INTO `order` (user_id, product_id, size, price, order_type, expiration_period, expiration_day)
    VALUES (#{userId}, #{productId}, #{size}, #{price}, #{type}, #{period}, #{day});
  </insert>

  <select id="findByOrderId" parameterType="java.lang.String" resultType="api.soldout.io.soldout.dtos.entity.OrderDto">
    SELECT *
    FROM `order`
    WHERE order_id = #{orderId};
  </select>

  <select id="findByUserId" parameterType="java.lang.String" resultType="api.soldout.io.soldout.dtos.entity.OrderDto">
    SELECT *
    FROM `order`
    WHERE user_id = #{userId};
  </select>

  <select id="findByProductId" parameterType="java.lang.String" resultType="api.soldout.io.soldout.dtos.entity.OrderDto">
    SELECT *
    FROM `order`
    WHERE product_id = #{productId};
  </select>

</mapper>